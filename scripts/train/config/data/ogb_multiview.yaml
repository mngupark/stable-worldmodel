dataset:
  name: ogbench/cube_single_multiview_expert.lance
  num_steps: ${eval:'${wm.num_preds} + ${wm.history_size}'}
  frameskip: 5
  keys_to_load:
    - pixels_front
    - pixels_side_pixels
    - pixels_side
    - pixels_front_pixels
    # - proprio/joint_pos
    # - proprio/joint_vel
    # - proprio/effector_pos
    # - proprio/effector_yaw
    # - proprio/gripper_opening
    # - proprio/gripper_vel
    # - proprio/gripper_contact
    - action
    - observation
  keys_to_merge:
    # proprio: proprio
    pixels: pixels
  image_columns:
    - pixels_front
    - pixels_side_pixels
    - pixels_side
    - pixels_front_pixels
# ['proprio/joint_pos', 'proprio/joint_vel', 'proprio/effector_pos', 'proprio/effector_yaw', 'proprio/gripper_opening', 'proprio/gripper_vel', 'proprio/gripper_contact',
# 'privileged/block_0_pos', 'privileged/block_0_quat', 'privileged/block_0_yaw', 'privileged/target_block', 'privileged/target_block_pos', 'privileged/target_block_yaw',
# 'prev_qpos', 'prev_qvel', 'qpos', 'qvel', 'control', 'time', 'target', 'success', 'render_time',
# 'pixels_front', 'pixels_front_pixels', 'pixels_side_pixels', 'pixels_side', 'observation', 'reward', 'terminated', 'truncated', 'action', 'id', 'proprio']
